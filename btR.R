#Chuong 2:
#Bài 1: S??? d???ng hàm c, t???o m???t bi???n ch???a các giá tr??? d??? dài c???a b???y con v???t. D???ng th???i t???o m???t bi???n ch???a các giá tr??? Tb. Bao g???m cái NAs. Chi???u dài trung bình c???a b???y con v???t là bao nhiêu?
LengthCT <- c(75, 85, 91.6,95, NA, 105.5,106)
Tb <- c(0,0,1,NA,0,0,0)
mean(LengthCT, na.rm = TRUE)
#Bài 2: S??? d???ng các hàm dim, nrow và ncol d??? xác d???nh s??? lu???ng d???ng v???t và các bi???n trong Heo r???ng.
Farm <- c(MO, MO, MO, MO, LN, SE, QM)
Month <- c(11,07,07,NA, 09, 09, 11)
#combine month, length, Tb
Boar <- cbind(Month, LengthCT, Tb)
dim(Boar)
ncol(Boar)
nrow(Boar)
#Bài 3: b???n có th??? làm di???u tuong t??? v???i hàm vecto? D???t tên khác cho vecto, ch???ng h???n nhu Tb2.
Tb2 <- vector(1,2,3)
#Bài 4: T???o ma tr???n sau trong R và xác d???nh chuy???n v???, ngh???ch d???o c???a nó và b???i D v???i ngh???ch d???o c???a nó (k???t qu??? ph???i là ma tr???n nh???n d???ng).
a.vec <- c(1,2,3)
b.vec <- c(4,2,1)
c.vec <- c(2,3,0)
vec <- cbind(a.vec,b.vec, c.vec)
#Bài 5: Thêm d??? li???u dã bi???n d???i can b???c hai vào khung d??? li???u. Th???c hi???n bài t???p tuong t??? v???i danh sách thay vì data.frame. s??? khác bi???t là gì?
df = data.frame(
  Farm,
  Month,
  Year <- c(00, 00, 01, NA, 03, 03, 02),
  Sex <- c(1, 2, 2, 2, 1, 2, 2),
  LengthClass <- c(1, 1, 1, 1, 1, 1, 1),
  LengthCT,
  Ecervi <- c(0, 0, 0, NA, 0, 0, 0),
  Tb,
  LengthCTsquare = LengthCT*LengthCT
)
df
#Bài 6: Nh???p kh???u d??? li???u vào R tru???c tiên b???ng cách s??? d???ng hàm read.table và sau dó là hàm quét. S??? d???ng hai tên khác nhau d??? luu tr??? d??? li???u. S??? khác bi???t là gì gi???a h???? S??? d???ng các hàm is.matrix và is.data.frame d??? tr??? l???i câu h???i này.
DL <- read.table(file = "ISIT.TXT", sep = "\t" )
is.matrix(DL)
is.data.frame(DL)
#Bài 7: Xu???t d??? li???u trong t???p Excel sang t???p ascii và nh???p d??? li???u dó vào R.
DT <- read.table(file = "Deer.txt")